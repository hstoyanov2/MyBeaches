import React from 'react';

const NotFoundPage = () => {
    return <h1 style={{display: 'flex', justifyContent: 'center', marginTop: '100px', color: 'white'}}>Resource is not found.</h1>
};

export default NotFoundPage;